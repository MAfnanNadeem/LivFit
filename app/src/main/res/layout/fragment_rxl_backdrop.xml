<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- BackLayout for BackDrop -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/backLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <include layout="@layout/fragment_reactions_filters" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Add BackdropBehavior to this view -->

    <FrameLayout
        android:id="@+id/frontLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#fff"
        android:clickable="true"
        android:elevation="4dp"
        android:focusable="true"
        app:layout_behavior="life.mibo.hexa.view.backdrop.BackdropBehavior">

        <include layout="@layout/fragment_reactions" />
    </FrameLayout>


</androidx.coordinatorlayout.widget.CoordinatorLayout>