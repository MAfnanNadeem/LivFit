package life.mibo.hardware.models;

public enum DeviceTypes implements BaseModel {
    GENERIC, BLE_STIMULATOR, WIFI_STIMULATOR, HR_MONITOR, SCALE, SCALE_OLD, TERMINAL, RXL_WIFI, RXL_BLE, RXT_WIFI, TEST, MI_SCALE
}
